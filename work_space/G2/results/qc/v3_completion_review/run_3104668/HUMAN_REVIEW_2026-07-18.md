# G1 V3 run_3104668 重点病例人工技术复核报告

## 1. 结论

- 复核病例：47 例。
- `pass_technical_visual`：47 例。
- 需条件放行：0 例。
- 需重建：0 例。
- 拒绝：0 例。

这 47 例在本次风险分层人工技术复核中未见空白体积、新增视野裁切、明显重影、块状伪影、大范围空洞、分割与影像粗大错位或可见 z 方向断层。结论仅表示“人工技术影像质控通过”，不是临床诊断，也不代表生成 T2W 与不存在的真实 T2W 完全等价。

## 2. 复核范围

| 告警类型 | 病例数 | 复核结果 |
|---|---:|---|
| `tiny_ratio_high` | 44 | 44 例通过 |
| `z_discontinuity` | 4 | 4 例通过 |
| 同时命中两类 | 1 | `BraTS-MET-00713-000`，两项均排除 |
| 去重合计 | 47 | 47 例通过 |

每例均检查：

1. t1c、生成 T2W、t2f 与 segmentation overlay 的轴位、冠状位和矢状位。
2. 最多 8 个最小/最大连通病灶的放大图。
3. 对 4 个 z 告警病例检查焦点前后 `z-4 / z-2 / z / z+2 / z+4` 切片。
4. 对靠近原始 FOV 边界的病灶额外核对 t1c、t2f、seg 是否也处于同一边界位置。

## 3. Tiny-lesion 复核

44 例中，每例连通病灶数为 1–73，tiny 病灶数为 1–68。`tiny_ratio_high` 是因受保护的原始 segmentation 中小于 27 mm3 的连通域占比超过 0.5，不是生成 T2W 自行创建了标签。

人工复核显示：

- segmentation 与 t1c/t2f/生成 T2W 位置一致，未见粗大空间错位。
- 未见因生成 T2W 导致的局部断层、黑块、亮块、重影或边界撕裂。
- 多个 tiny focus 位于脑表面或原生 FOV 边界附近，在受保护的 t1c/t2f/seg 中也处于同一位置，不是生成过程新增的裁切。
- `BraTS-MET-00751-000` 和 `BraTS-MET-00775-000` 的复核焦点靠近原始 FOV 边缘，已额外回看三平面，未见 generated-only truncation。

因此 44 例的 `tiny_ratio_high` 均可作为已人工排除的软告警。

## 4. Z 连续性复核

| 病例 | z continuity | area smoothness | intensity smoothness | 人工结论 |
|---|---:|---:|---:|---|
| `BraTS-MET-00558-000` | 0.4351 | 0.1696 | 0.7006 | 相邻层脑轮廓、脑室和病灶邻域平滑变化，无可见断层 |
| `BraTS-MET-00713-000` | 0.3056 | 0.2308 | 0.3804 | 两个 tiny 病灶且 ROI 极小；完整 z-stack 无重复层或突变 |
| `BraTS-MET-00754-000` | 0.4479 | 0.0933 | 0.8024 | 脑室及周围解剖结构连续，无单层异常亮暗跳变 |
| `BraTS-MET-00765-000` | 0.4844 | 0.2153 | 0.7534 | 病灶体积随 z 层正常出现和消失，未见生成块突然出现 |

这 4 例的数值均低于当前 0.5 筛查阈值，但它们未见可见的 z 方向失败。该指标在小 ROI、脑轮廓随层面积快速变化或强度分布变化时较敏感，因此本次将其判定为已人工排除的软告警。

## 5. 共性观察与限制

1. 生成 T2W 的纹理普遍比 t1c/t2f 平滑，但在这 47 例中未见局部突兀的阻断性伪影。
2. 阶段 6 病例本来没有可靠的真实 T2W，因此本次是 no-reference 技术复核，不能证明生成 T2W 的病理信号完全真实。
3. Stage-5 验证已记录平滑与分割敏感性风险；阶段 6 是在操作者明确风险接受后生成。
4. 本次通过代表这 265 例可进入受控的 S2 完整模态消融实验，不代表可直接替代真实 T2W 或作为临床数据发布。
5. 最终保留与否必须由固定 split 的 `real-only` 对 `real + V3 completion` 消融结果决定。

## 6. 审计产物

- 逐例判定：`review_index.csv`
- 47 张三平面总览：`montages/`
- 47 张病灶放大图：`lesions/`
- 4 张相邻层连续性图：`z_stacks/`
- 47 张病例联系面板：`contact_panels/`
- 12 张联系表：`contact_sheets/`

全部逐例决策均已写为 `pass_technical_visual`。

## 7. 审批后派生结果

人工复核后已通过 run root 的 `g2_approval_manifest.csv` 重跑 G2 intake，未直接编辑 accepted 产物。派生结果为：

| 状态 | 病例数 |
|---|---:|
| `accepted_for_training` | 212 |
| `accepted_for_evaluation` | 53（27 val + 26 locked test） |
| `pending_review` | 0 |
| `rejected` | 0 |
| `needs_regeneration` | 0 |

47 例已复核软告警通过 `cleared_review_reasons` 逐项清除；未用模糊的全局 override 清除空白、错位或伪影告警。
